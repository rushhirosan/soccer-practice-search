# 今後の改善タスク

## 最優先（UX改善）

### 1. モバイル対応の確認（要実機確認）
- 実施内容（2026-03-29）
  - **コードレビュー**: 全ページ `viewport` あり、`@media` (1024 / 768 / 480)、タッチ `touch-action: manipulation`、入力 `font-size: 16px`（iOS ズーム防止）、メニューハンバーガー・sticky 検索の実装を確認。
  - **本番 HTTP 計測**（モバイル UA、`curl`）: 初回 HTML 応答はコールドスタート等で **約 4.5s**（TTFB 約 4.4s）、続くリクエストは **約 0.47s**。静的 CSS は **約 0.54s**（ウォーム時）。実機はウォーム時は軽快に見える想定。
  - **修正**: モバイルで検索バーが `sticky` でヘッダー（z-index より上に視認できない位置）に隠れる問題を、`top` をヘッダー相当オフセット + `safe-area-inset-top` で補正。フッターに `safe-area-inset-bottom`。`viewport-fit=cover`。ハンバーガー **44px** タップ目安。
  - **本環境では Node 未導入のため Lighthouse 未実行**。実機または Chrome DevTools の「モバイル」で Performance / Lighthouse を推奨。
- 実機で最終確認したい項目（チェックリスト）
  - [ ] ホーム: スクロール時に検索バーがヘッダー直下で見えるか
  - [ ] ボード・メモ帳・お気に入り: タップ・スクロール・入力
  - [ ] ノッチ・ホームインジケータ付き端末でフッター・固定 UI が窮屈でないか

※ 対応済み: アクセシビリティ（フォーカス表示）、リアルタイム検索(debounce)、検索履歴(localStorage)、検索候補（履歴から選択）、タッチ操作最適化

---

## 高優先（本番環境の安定性）

### 4. バックアップ機能
- PostgreSQLの定期バックアップスクリプト
- Fly.io環境での自動バックアップ設定
- バックアップの復元手順

### 5. ログ監視の強化
- ファイルログの実装（ログローテーション含む）
- エラーログの集約・通知
- パフォーマンスメトリクスの記録

### 6. サーバー側エラーハンドリング強化
- エラータイプ別の詳細メッセージ
- ユーザーフレンドリーなエラー表示（"Database error"以外）

---

## 中優先（運用・体験向上）

### 7. データ更新の自動化
- Cronジョブまたはスケジューラーの設定
- 新規動画の自動検出・追加
- 更新失敗時の通知

### 8. 検索機能の高度化
- 検索候補・オートコンプリート（サーバー側）
- 検索結果の並び替えオプション拡張
- 複数条件のAND/ORフィルタリング

### 9. パフォーマンス監視
- 検索クエリの実行時間計測
- スロークエリの検出・ログ記録

### お気に入りの多デバイス同期（未着手・後回し）
- **現状**: お気に入りは `localStorage` のみ（このブラウザ内。他端末とは同期しない）
- **将来案**: ユーザー認証（ログイン）＋ PostgreSQL 等に `user_id` と紐づけて保存する API（一覧／追加／削除）。または Firebase / Supabase など BaaS で認証＋クラウドストア
- **セットで検討**: プライバシーポリシー更新、セッション／トークン管理、既存ローカルお気に入りの取り込み（任意）の有無
- **注意**: 今はやらない。着手するときに設計から決める

---

## 少年サッカーコーチ向け機能

### 10. 戦術ボードと練習検索の連携
- 検索で見つけた練習メニューを戦術ボードで図解し、セットで保存・印刷
- 練習カードに「戦術ボードで開く」ボタン（図解付きメモとの紐づけ）

### 11. 定番メニュー集
- 年代別・テーマ別のメニュー集（低学年向け、高学年向け、雨の日・体育館など）
- ウォーミングアップ／クールダウンの定番5〜10本
- 既存検索との連携

### 12. 練習メモ・メニュー帳
- 検索結果を選んでメモ・印刷できる機能
- メニュー＋練習ボード図をセットで記録・印刷
- 練習計画の記録・保存

### 13. 検索の少年コーチ向け強化
- 年代別フィルタ（「低学年向け」「高学年向け」など、より細かい対象年代）
- 練習時間でフィルタ（10分以内など、時間の限られるコーチ向け）
- 動画の再生時間を目立つように表示

### 14. 練習計画テンプレート
- 週1回／週2回の練習計画のテンプレート
- 年間カレンダー（大会・遠征・オフを入れた見本）
- 1回の練習の構成例（ウォームアップ→メイン→ゲーム形式）

### 15. 指導ノウハウ・保護者対応の記事
- 年代別の指導のポイント
- 保護者対応、声かけ・褒め方
- 練習計画の立て方

### 16. 大会・審判・チーム運営
- 大会前チェックリスト（提出物、持ち物、保護者への連絡）
- 審判の基礎（少年でよくある笛のパターン、保護者向けの短い解説）
- チーム運営（当番表、お茶当番の例、保護者への連絡テンプレ）

### 17. 戦術ボードの共有
- 作戦をURLで共有（閲覧専用リンクを発行して保護者や選手に送る）
- PNG／PDFでのエクスポート強化（既存のPNG保存を発展）

### 18. よくある悩みQ&A
- 「人数が少ないときの練習」「上手い子とそうでない子の差が大きい」「練習に来ない子への対応」など
- 記事形式で掲載（運用コストを抑える）

---

## 将来検討

### 19. 多言語対応
- i18n対応、英語版UI

### 20. 通知機能
- RSSフィード、新着動画のハイライト

### 21. 分析機能
- 検索キーワードの統計、人気動画の分析

### 22. キャッシュ機能
- アクセス数増加時に検討

### 23. モバイルアプリ
- オフライン機能・プッシュ通知が必要になった時に検討

### 24. API拡張
- 外部アプリケーション向けRESTful API

---

## 推奨実装順

| Phase | タスク |
|-------|--------|
| 1 | モバイル実機確認（※他は対応済み） |
| 2 | バックアップ、ログ監視、サーバー側エラーハンドリング |
| 3 | データ更新の自動化、検索機能の高度化、パフォーマンス監視 |
| 4 | 戦術ボード連携、検索の少年コーチ向け強化、定番メニュー集、練習メモ帳 |
| 5 | 練習計画テンプレ、指導記事、大会・審判・運営、戦術ボード共有、Q&A |
| 6 | 多言語、通知、分析（必要に応じて） |

---

## チャンネル構成（参考）

### 利用中
- 雑誌『サッカークリニック』, web版
- COACH UNITED編集部
- SOLUNA Ch.
- サッカーのみちしるべ
- REGATEドリブル塾
- ゲキサカ

### 未使用
- サカサポChannel
- KSS SOCCER SCHOOL
- サカイク編集部
- Footy14Skills
- イースリーショップ
